# nagamachi-rinri-katsudo-keikaku

仙台長町倫理法人会の年間活動計画ワークシート（静的な `index.html` 1ファイル、GitHub Pagesで配信）。
データはPIN + Google Apps Script バックエンド（`apps-script/`）経由でGoogle Driveに保存される。

## バージョン表記

`index.html` 内の `var APP_VERSION = "x.y.z";`（ステータスバーの「⋮」メニュー内に表示）は、
**仕様・挙動に変更を加えたコミットごとに更新すること。**

- バグ修正・見た目の微調整のみ: パッチ番号を上げる（例: 1.0.0 → 1.0.1）
- 新機能追加・挙動変更: マイナー番号を上げる（例: 1.0.1 → 1.1.0）
- ドキュメントのみの変更や、ユーザーに見える変更が一切ない場合は据え置きでよい
